# ex-05-06 — 도메인 분기형 references 시연

책 다이어그램을 mermaid + 실물 sql-query 스킬 디렉토리로 재현하고, 2건 호출 트레이스로 조건부 로드를 시각화 (extend).

## 보기
`result/quote.md`(메뉴판/레시피 비유), `result/diagram.md`(mermaid), `.claude/skills/sql-query/{SKILL.md, references/{finance,inventory}.md}`, `result/traces/{q4-sales,inventory-low}.md`, `result/extension/`(marketing.md 추가), `result/checklist.md`.
