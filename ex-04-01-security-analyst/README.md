# ex-04-06 — security analyst

책 security-analyst(읽기 전용) 에이전트를 작성하고 의도적 취약 Flask 앱에 적용해 보고서를 산출 (extend).

## 실행 (메인 batch)
```
claude --agent security-analyst "sample-app/ 의 보안 취약점을 분석하라"
```
결과는 `result/security-report.md`, tools 강제 확인은 `result/tools-enforcement-check.md`.
